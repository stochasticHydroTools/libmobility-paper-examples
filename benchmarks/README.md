# Benchmarks

The scripts in this folder will reproduce the benchmark data presented in the article.

To generate the performance data:

```bash
$ python benchmark.py
```
A CSV file will be generated.

The notebook runs the benchmarks and also has a visualization of the results.
